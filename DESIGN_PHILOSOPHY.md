# Obsidian JIRA Task Manager - 設計思想 (Design Philosophy)

このドキュメントは、本プラグイン（`obsidian-task-manager`）の目指すビジョン、アーキテクチャの根幹、および今後の機能拡張の指針を記録したものです。

---

## 1. コアビジョン：AI協調 ＆ 打鍵最小化 (Minimal Typing & AI Copilot)

一般的なタスク管理ツールは、人間が手動で詳細なサブTODOを入力し、カレンダーを見ながら日付を設定する作業負担が発生します。
本プラグインでは、**「人間が極力文字を打たない」** タスク管理体験を目指します。

1. **人間の役割**:
   - やりたいことの大枠（タイトルや抽象的な目標）を1行入力する。
   - AIが提案した「サブTODO」や「実施スケジュール」を見て、直感的に調整・承認する。
2. **AIの役割**:
   - **TODO自動分解 (Breakdown)**: 大枠のタスクから具体的な実施ステップ（サブTODO）を自動生成。
   - **自動スケジューリング & 再配置 (Reschedule)**: 締め切り (`due`) から逆算し、空いている日（`scheduled`）にタスクを分散。進捗遅延が発生した場合は無理のない調整案を提示。

---

## 2. データ構造：1-Task-1-Note ＆ 単一エンティティモデル (Single Entity Model)

タスクの階層化にあたり、`Epic` / `Task` / `Subtask` という固定のクラス階層を作らず、**すべての単位を単一の `Task` ノート**として扱います。

### なぜ単一エンティティ＋親参照 (`parent`) なのか？
- **柔軟な昇格・降格**:
  - タスク管理において「ただのTaskだと思っていたものが膨らんでEpic化した」「EpicにするほどではなかったのでTaskに戻した」という粒度の変化は日常茶飯事です。
  - 親タスクの参照 (`parent: "TASK-001"`) やマーカー (`type: "epic" | "task" | "subtask"`) を変えるだけで、ノートの破壊やデータ移行なしに階層構造を自在に変更できます。
- **Obsidianノートとの高い親和性**:
  - 各タスクが1つのMarkdownファイルとして独立しているため、詳細なメモ、Webリンク、Vault内の他ノートへのバックリンクを制約なく埋め込むことができます。

---

## 3. スケジューリング設計：「締め切り (Due)」と「実施予定日 (Scheduled)」の分離

タスク管理が破綻する原因の多くは、「締め切り日」と「実際に作業する日」が混同されることです。

- **`due` (Due Date / 最終締め切り)**: この日までに終わっていなければならない期日。
- **`scheduled` (Scheduled Date / 実施予定日)**: 今日、明日、あるいは特定の日に実際に作業を行う日。

AIはこの `due` と `scheduled` を観察し、`scheduled` が過去に置き去りにされている（未完了のまま過ぎた）場合に、`due` に間に合うように `scheduled` を未来の日に自動で再配置・提案します。

---

## 4. UI/UX 設計：複数ビューとワンタップ操作

1. **Status Board (Kanban)**:
   - 「To Do」「In Progress」「Done」のステータス管理。
   - 親タスクカードの配下に、所属する子タスクをネスト（インデント）表示。
   - ボタン1つで親タスクの直下に子タスクノートを作成可能 (`+ Subtask`)。
2. **Schedule Board (Timeline / Action Date)**:
   - 「Overdue (期限切れ)」「Today」「Tomorrow」「This Week」「Later」の実施日別カンバン。
   - 今やるべきタスクに集中できるUI。
3. **AI アクション（今後追加予定）**:
   - `[ ✨ AI Breakdown ]`: 子タスクの自動分解生成。
   - `[ 🔄 AI Reschedule ]`: 実施日の自動再最適化。

---

この設計思想に基づき、段階的（スモールスタートベース）に実装・進化させていきます。

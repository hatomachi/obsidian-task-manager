# Obsidian JIRA Task Manager - 設計思想 (Design Philosophy)

このドキュメントは、本プラグイン（`obsidian-task-manager`）の目指すビジョン、アーキテクチャの根幹、および今後の機能拡張の指針を記録したものです。

---

## 1. コアビジョン：単なるタスク作成から「AIスクラムマスター (行動伴走パートナー)」へ

一般的なタスク管理ツールは、人間が手動で詳細なサブTODOを入力し、カレンダーを見ながら日付を設定する作業負担が発生し、「抽象的で大きなタスク」が放置されがちでした。
本プラグインでは、**「人間がすぐ行動に移せるよう、15〜30分の具体的物理行動 (Next Physical Action) へ強制分解し伴走する」** 世界観を目指します。

1. **人間の役割**:
   - やりたいことの大枠（タイトルや抽象的な目標）を1行入力する。
   - AIが提案した「15〜30分で終わる具体的物理行動」を見て、直感的に調整・承認する。
2. **AI（スクラムマスター）の役割**:
   - **日本語出力の絶対強制**: すべての生成タイトル・解説テキストは自然な日本語で生成。
   - **Next Physical Action への強制分解**: 曖昧な表現（「〜の検討」「〜の調整」「〜の確認」等）を一切禁止し、「〜を開く」「〜を1行書く」「〜をブラウザで検索する」といった15〜30分単位の日本語物理行動へ強制変換。
   - **プロンプトモジュールの完全分離 (`src/prompts/`)**: AIへのプロンプト文をコード直書きから分離し、専用ディレクトリで保守・管理。
   - **カスタムルールの動的注入**: 設定画面や Vault 内のカセットからユーザー独自の行動制約・SOPを読み込んで伴走。
   - **対話型タスク再構築 (Refinement)**: 人間の自然言語指示に従い、子タスク・孫タスクの追加・変更・削除を提案。

---

## 2. アーキテクチャ：プロンプトモジュールの分離構造 (`src/prompts/`)

プロンプトのメンテナンス性向上と日本語化強制のため、AI呼び出しロジック (`AIService.ts`) からプロンプト文を完全分離しています。

```text
src/prompts/
├── systemRules.ts            <-- 共通ルール（日本語出力強制・NPA物理行動制約・曖昧語禁止）
├── taskBreakdownPrompt.ts    <-- タスク分解 (Breakdown) プロンプト
├── taskRefinePrompt.ts       <-- 対話壁打ち (Refine) プロンプト
├── taskReschedulePrompt.ts   <-- スケジュール再配置 (Reschedule) プロンプト
└── index.ts                  <-- モジュール集約
```

---

## 3. Human-in-the-Loop AIスクラムマスター：作戦策定 ＆ インタラクティブプレビュー

お題からいきなり大量の未検証タスクを一括自動作成する従来のアプローチは、依存関係の破綻や不確実性の考慮不足を招くため排除しています。
本プラグインでは、**【①作戦（ボトルネック分析）の策定】➔【②人間によるプレビューとインタラクティブな修正】➔【③承認されたPhase 1タスクのみをノートへ書き込み】** という Human-in-the-Loop 構成を採用しています。

---

## 4. 安全装置：ワンタップ Undo スナップショット (Snapshot Rollback)

AIにタスク群を一括修正させたり、スケジュールを自動調整させたりする際、人間が安心してAIに委ねられるための安全装置です。画面上のボタン1つでAI実行前の状態に一括ロールバックできます。

---

## 5. データ構造：2レイヤー管理（ワークフォルダ ＋ インラインタスク）

すべてのタスクを1ノート化する「1-Task-1-Note」はファイル増殖とコンテキスト分散を招くため廃止し、**「実行単位（重タスク）」と「手順（軽タスク）」の2レイヤー構造**を採用します。

1. **Layer 1: ワークフォルダ（`_task_works/<task-id>/index.md`）**:
   - 成果物や作戦会議を伴うプロジェクト単位。
   - ASCII + タイムスタンプの衝突レス命名規則（例: `_task_works/20260810-ndp-sw-upgrade/index.md`）。
2. **Layer 2: インラインタスク（`index.md` 内の `- [ ]`）**:
   - ワークフォルダ内の `index.md` 本文に並ぶ具体的物理行動（Next Physical Action）。
   - 既存の `TaskService.ts` によるパース・Undo機構をそのまま適用。

---

## 6. スケジューリング設計：「締め切り (Due)」と「実施予定日 (Scheduled)」の分離

- **`due` (Due Date / 最終締め切り)**: この日までに終わっていなければならない期日（Frontmatter: `due_date`, または箇条書きの `📅`）。
- **`scheduled` (Scheduled Date / 実施予定日)**: 今日、明日、あるいは特定の日に実際に作業を行う日。

---

## 7. カセット（タスクパターン）読み込み＆プロンプト注入エンジン

組織や個人の洗練された業務手順（SOP）・絶対制約・標準成果物を AI に注入してタスクを遂行させる拡張機構です。

### ① 思考モデル：クラス（型）とインスタンス（実体）

- **カセット (1フォルダ = 1クラス)**:
  - Vault 内の `settings.patternFolderPath`（デフォルト: `_task_patterns/<pattern-id>/`）に配置される再利用可能な業務ルールパッケージ。
  - ルール (`pattern.md`)、成果物の雛形 (`templates/`), 成功事例 (`examples/`) を同封した読み取り専用の「型」。
- **1カセット = 1フォルダ統一規則**:
  - 単一 Markdown ファイル形式との複雑な分岐を排除し、「1カセット ＝ 1フォルダ」のシンプルな構造に統一。

---

## 8. ワークフォルダエンジン（`_task_works/`）＆ 成果物展開

カセット（タスクパターン）が適用されたタスクは、専用の作業領域（ワークフォルダ）を生成して人間とAIの協調環境を提供します。

- **フォルダ自動生成**: タスク作成/昇格時に `_task_works/<task-id>/` を生成。
- **テンプレート展開**: 適用されたカセットの `templates/` 配下にある Markdown 成果物（例: `release-plan.md`）をワークフォルダ内へ初期配置。
- **コンテキスト同封**: ワークフォルダ内に配置されたインプット資料や成果物ドキュメントを、AIが参照・更新して成果物を完成させる。

---

## 9. 階層構造（Theme -> Initiative）と「昇格（Promote）」機構

「Mythosセキュリティ対策」のような巨大なテーマから末端の実行手順までを、Wikiリンクで疎結合に接続します。

1. **`parent_task` によるバックリンク**:
   - 子ワークフォルダの `index.md` Frontmatter に `parent_task: "[[_task_works/<parent-id>/index]]"` を自動保持。
   - 本文冒頭にも `## 📌 親タスク / テーマ` セクションとして親ノートへの Wiki リンク (`- [[_task_works/<parent-id>/index|親タスクタイトル]]`) を動的挿入し、上位の文脈（Why）を人間および AI が常時参照可能にする。
2. **サブタスクの昇格 (`Promote Subtask to Work Folder`)**:
   - エディタコマンド `Obsidian JIRA: Promote Subtask to Work Folder` を実行することで、カーソル行のインラインタスク（`- [ ] サブタスク`）を解析。
   - タグ（例: `#リリース`）から `PatternService` で該当するカセットを自動判定し、`_task_works/<child-folder-id>/` 内へカセットテンプレートを展開。
   - 親ノート上の元行を、子ワークフォルダへの Wiki リンクに自動置換（例: `- [ ] #リリース [[_task_works/<child-id>/index|決済APIの仕様書を確認する]] 📅 2026-08-15`）。
   - 昇格完了後に Notice 通知を出し、生成された子 `index.md` をエディタで自動展開。

---

## 10. 期限管理（Due / Scheduled）とリスク検知ビューア

カセットの制約と日付アノテーション（`due_date`, `📅`）を組み合わせ、自動で「テコ入れが必要なタスク」を算出します。

- **メタデータ**: ワークフォルダ単位の期日は Frontmatter (`due_date`)、インラインタスクの期日は `📅 YYYY-MM-DD` で管理。
- **リスク検知ロジック (At-Risk Alert)**:
  - 期限まで残りわずか（例: あと1日）にもかかわらず、未完了サブタスクが多数残っている。
  - カセットの必須フェーズ（例: 「グループレビュー」）が完了していない状態で申請期限が迫っている。

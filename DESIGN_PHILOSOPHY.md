# Obsidian JIRA Task Manager - 設計思想 (Design Philosophy)

このドキュメントは、本プラグイン（`obsidian-task-manager`）の目指すビジョン、アーキテクチャの根幹、および今後の機能拡張の指針を記録したものです。

---

## 1. コアビジョン：単なるタスク作成から「AIスクラムマスター (行動伴走パートナー)」へ

一般的なタスク管理ツールは、人間が手動で詳細なサブTODOを入力し、カレンダーを見ながら日付を設定する作業負担が発生し、「抽象的で大きなタスク」が放置されがちでした。
本プラグインでは、**「人間がすぐ行動に移せるよう、15〜30分の具体的物理行動 (Next Physical Action) へ強制分解し伴走する」** 世界観を目指します。

1. **人間の役割**:
   - やりたいことの大枠（タイトルや抽象的な目標）を1行入力する。
   - AIが提案した「15〜30分で終わる具体的物理行動」を見て、直感的に調整・承認する。
2. **AI（スクラムマスター）の役割**:
   - **日本語出力の絶対強制**: すべての生成タイトル・解説テキストは自然な日本語で生成。
   - **Next Physical Action への強制分解**: 曖昧な表現（「〜の検討」「〜の調整」「〜の確認」等）を一切禁止し、「〜を開く」「〜を1行書く」「〜をブラウザで検索する」といった15〜30分単位の日本語物理行動へ強制変換。
   - **プロンプトモジュールの完全分離 (`src/prompts/`)**: AIへのプロンプト文をコード直書きから分離し、専用ディレクトリで保守・管理。
   - **カスタムルールの動的注入**: 設定画面や Vault 内のルールファイル (`task-rules.md` 等) からユーザー独自の行動制約を読み込んで伴走。
   - **対話型タスク再構築 (Refinement)**: 人間の自然言語指示に従い、子タスク・孫タスクの追加・変更・削除を提案。
   - **自動スケジューリング & 再配置 (Reschedule)**: 締め切り (`due`) から逆算し、空いている日（`scheduled`）にタスクを分散。進捗遅延が発生した場合は無理のない調整案を提示。

---

## 2. アーキテクチャ：プロンプトモジュールの分離構造 (`src/prompts/`)

プロンプトのメンテナンス性向上と日本語化強制のため、AI呼び出しロジック (`AIService.ts`) からプロンプト文を完全分離しています。

```text
src/prompts/
├── systemRules.ts            <-- 共通ルール（日本語出力強制・NPA物理行動制約・曖昧語禁止）
├── taskBreakdownPrompt.ts    <-- タスク分解 (Breakdown) プロンプト
├── taskRefinePrompt.ts       <-- 対話壁打ち (Refine) プロンプト
├── taskReschedulePrompt.ts   <-- スケジュール再配置 (Reschedule) プロンプト
└── index.ts                  <-- モジュール集約
```

---

## 3. Human-in-the-Loop AIスクラムマスター：作戦策定 ＆ インタラクティブプレビュー

お題からいきなり大量の未検証タスクを一括自動作成する従来のアプローチは、依存関係の破綻や不確実性の考慮不足を招くため排除しています。
本プラグインでは、**【①作戦（ボトルネック分析）の策定】➔【②人間によるプレビューとインタラクティブな修正】➔【③承認されたPhase 1タスクのみをノートへ書き込み】** という Human-in-the-Loop 構成を採用しています。

1. **モーダル UI State Machine (`AICopilotModal.ts`)**:
   - `STATE_INPUT`: お題（例: 「名古屋旅行（ジブリ・レゴランド）」）入力。
   - `STATE_GENERATING`: ボトルネック分析 & Phase 1 作戦策定中（スピナー表示）。
   - `STATE_PREVIEW`: (ノート未書き込み状態) 作戦メモ Callout ＋ 編集可能な Phase 1 タスク一覧（チェック切り替え、タイトル直編集、追加・削除） ＋ フィードバック修正指示。
   - `STATE_COMMITTED`: 人間が「確定」した最終編集結果を作戦書き込み用ターゲットノートへ永続化。
2. **書き込み先ターゲット決定優先アルゴリズム (`TaskService.ts`)**:
   - **第1優先 (開き元ノート)**: タスクカードの `✨ AI` ボタンから起動された場合、その元のタスクノート (`targetFile`) へ直接追記。
   - **第2優先 (アクティブエディタ)**: アクティブな Markdown エディタが開いている場合、そのエディタへ追記。
   - **第3優先 (同名既存ノート)**: お題タイトルと一致する既存タスクノートが存在する場合、そのノートへ追記。
   - **第4優先 (新規自動作成)**: 上記いずれも存在しない場合のみ、お題タイトルの 1-Task-1-Note を新規作成して書き込み。
3. **Markdown 出力フォーマット規格**:
```markdown
> [!strategy] AIスクラムマスターの作戦メモ
> - **最優先ボトルネック**: {ボトルネックの分析}
> - **依存関係**: {何が決まれば何が決まるかの流れ}
> - **基本方針**: {実行戦略}

## 📍 Phase 1: ボトルネック・不確実性の解消
- [ ] {タスク1: 15〜30分で終わる具体的物理行動}
- [ ] {タスク2: ...}
```
4. **プロンプト制約 (`strategyPrompt.ts`)**:
   - 全タスクを一発生成せず、不確実性を潰すための「Phase 1（最初の1〜3個）」のみ生成。
   - 動詞は必ず「〜を開く」「〜を入力する」等の具体的物理行動（Next Physical Action）で開始し、「〜の検討」「〜の調整」などの曖昧表現を一切禁止。


---

## 4. 安全装置：ワンタップ Undo スナップショット (Snapshot Rollback)

AIにタスク群を一括修正させたり、スケジュールを自動調整させたりする際、人間が安心してAIに委ねられるための**安全装置**です。

- **自動スナップショット**:
  - AIがノート群を書き換える直前の全タスクの属性（タイトル, `status`, `scheduled`, `due`, `parent`）を記憶。
- **ワンタップ巻き戻し (`[ ↩️ Undo AI Action ]`)**:
  - 変更結果が気に入らなければ、画面上のボタン1つでAI実行前の状態に一括ロールバック（新規作成ノートの削除・既存属性の完全復元）。

---

## 5. データ構造：1-Task-1-Note ＆ 衝突レス命名規則 (ASCII + Timestamp + Jitter)

### ID ＆ ノートファイル名の命名ルール
マルチデバイス同期、チーム利用、Git運用における衝突可能性を実質ゼロにし、OS間でのパス互換性を保証するため、完全ASCIIの命名規則を採用しています。

- **ID ＆ ファイル名生成フォーマット**:
  - `${idPrefix}${YYYYMMDDHHmmss}-${jitter}.md`
  - 例: `TASK-20260808225340-a8f3.md`
- **タイトルとIDの分離**:
  - ノートのタイトル（日本語や長文）は Frontmatter (`title: "..."`) および本文 Heading (`# タイトル`) で管理し、ファイル名は純粋な唯一性キーとします。

---

## 6. スケジューリング設計：「締め切り (Due)」と「実施予定日 (Scheduled)」の分離

タスク管理が破綻する原因の多くは、「締め切り日」と「実際に作業する日」が混同されることです。

- **`due` (Due Date / 最終締め切り)**: この日までに終わっていなければならない期日。
- **`scheduled` (Scheduled Date / 実施予定日)**: 今日、明日、あるいは特定の日に実際に作業を行う日。

AIはこの `due` と `scheduled` を観察し、`scheduled` が過去に置き去りにされている（未完了のまま過ぎた）場合に、`due` に間に合うように `scheduled` を未来の日に自動で再配置・提案します。

---

## 7. カセット（タスクパターン）読み込み＆プロンプト注入エンジン

単に AI の素の知識でタスクを分解する段階から、「組織や個人の洗練された業務手順（SOP）・絶対制約・標準成果物」を AI に注入してタスクを遂行させる拡張機構です。

### ① 思考モデル：クラス（型）とインスタンス（実体）
- **カセット (1フォルダ = 1クラス)**:
  - Vault 内の `settings.patternFolderPath`（デフォルト: `_task_patterns/<pattern-id>/`）に配置される再利用可能な業務ルールパッケージ。
  - ルール (`pattern.md`)、成果物の雛形 (`templates/`), 成功事例 (`examples/`) を同封した読み取り専用の「型」。
- **1カセット = 1フォルダ統一規則**:
  - 単一 Markdown ファイル形式との複雑な分岐を排除し、「1カセット ＝ 1フォルダ」のシンプルな構造に統一。

### ② カセットフォルダ構造規格
```text
_task_patterns/
└── release-prep/                   <-- pattern.id = "release-prep"
    ├── pattern.md                  <-- 必須: Frontmatter (id, name, trigger_tags) + 必須ワークフロー + 絶対制約
    ├── templates/                  <-- 任意: 成果物テンプレート (.md)
    │   └── release-plan.md
    └── examples/                   <-- 任意: Few-shot事例 (.md)
        └── past-release-01.md
```

### ③ 動的読み込み ＆ タグ自動マッチング (`PatternService.ts`)
- **ピンポイント走査**: Vault 全体の再帰走査を行わず、`settings.patternFolderPath` 配下のみを高速・安全に走査。
- **タグ抽出 & マッチング**: タスクのタイトル（例: `#リリース`）や Obsidian `metadataCache` からタグ配列を抽出し、カセットの `trigger_tags`（例: `["#リリース", "#release"]`）と比較して合致するカセットを自動ロード。
- **フォールバック設計 (Graceful Degradation)**: カセット未存在時や `templates/`, `examples/` 欠損時も例外でクラッシュせず、安全に空要素としてフォールバック処理。

### ④ プロンプト注入アーキテクチャ (`src/prompts/`)
- ビジネスロジック内へのプロンプト文字列直書きを厳禁とし、プロンプトモジュール (`taskBreakdownPrompt.ts`, `strategyPrompt.ts`, `taskRefinePrompt.ts`) にプロンプト生成責務を集約。
- マッチしたカセットのSOP（必須ワークフロー、絶対制約、成果物テンプレート、Few-shot事例）を `【適用される強固な業務パターン・絶対制約（SOP）】` として AI へ動的注入。

